/**
 * Tokens are CSS-only — the source of truth is colors.css + tokens.css, consumed
 * via `import 'designoslav/tokens.css'` and referenced as var(--do-*).
 * This module exists for future token helpers (e.g. a typed list of token names);
 * it intentionally holds no duplicated color values.
 */
export {};
